{
  "name": "@go-admin-plus/workspace",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "dependencies": {
    "@go-admin-plus/adapter-browser": "workspace:*",
    "@go-admin-plus/app-shell": "workspace:*",
    "@go-admin-plus/platform": "workspace:*",
    "@go-admin-plus/ui": "workspace:*"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @go-admin-plus/admin-web dev",
    "build": "corepack pnpm --filter @go-admin-plus/admin-web build && corepack pnpm --filter @go-admin-plus/admin-desktop build",
    "build:prod": "corepack pnpm build",
    "lint": "oxlint --ignore-pattern '**/node_modules/**' --ignore-pattern '**/dist/**' --deny-warnings apps packages tests",
    "typecheck": "corepack pnpm --recursive --if-present typecheck && vue-tsc -p tests/shell/tsconfig.json && vue-tsc -p tests/e2e/audit/tsconfig.json && vue-tsc -p tests/e2e/files/tsconfig.json && vue-tsc -p tests/e2e/iam/administration/tsconfig.json && vue-tsc -p tests/e2e/iam/session/tsconfig.json && vue-tsc -p tests/e2e/scheduler/tsconfig.json && vue-tsc -p tests/e2e/web-shell/tsconfig.json",
    "type-check": "corepack pnpm typecheck",
    "test": "vitest run --config tests/shell/vitest.config.ts && node tests/shell/node-tests.mjs",
    "test:unit": "corepack pnpm test",
    "test:e2e:iam-session": "node tests/e2e/iam/session/run.mjs",
    "test:e2e:desktop": "node --test tests/e2e/desktop/run.test.mjs && node tests/e2e/desktop/run.mjs",
    "check:workspace": "node --test tests/shell/workspace-boundary.test.mjs",
    "verify": "corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm test:e2e:desktop && corepack pnpm check:workspace && corepack pnpm build"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitejs/plugin-vue": "catalog:",
    "@vue/compiler-sfc": "catalog:",
    "@vue/test-utils": "catalog:",
    "happy-dom": "catalog:",
    "oxlint": "catalog:",
    "sass": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vue-tsc": "catalog:",
    "vue": "catalog:"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.1.3"
}
